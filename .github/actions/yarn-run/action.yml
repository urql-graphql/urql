name: 'Run a Yarn command'
description: 'Locally run a forked Yarn command as an action.'
inputs:
  command:
    description: 'Command'
    default: 'help'
runs:
  using: 'node16'
  main: 'action.js'
