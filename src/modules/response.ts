import { IResponse, IProcessedResponse } from '../interfaces/response';

const processResponse = (response: IResponse): Promise<IProcessedResponse> => {
  return Promise.reject(new Error('TODO'));
};
